// Converting Number to String
let age = 20;
console.log(typeof age);

// By just adding just string after number will make variable data type to string
// age = age + "";
// console.log(typeof age);

// Good way of converting number to string is String(num)

age = String(age);
console.log(typeof age);
