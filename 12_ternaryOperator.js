// Ternary Operator.

let age = 13;
// using if else
  let dirnk;
  if(age>=5){
    drink = "coffee";
  }else{
    drink = "milk";
  }
  console.log(drink);
// ternary operator / conditional Operator
  let drive = age >= 12 ? "Bike" : "Cycle";
  console.log(drive);
